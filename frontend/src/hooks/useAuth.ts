'use client';

import { useAuth } from '@/context/auth-context';

/**
 * Thin re-export so you can import from either path.
 * Usage:
 *   import { useAuth } from '@/hooks/useAuth';
 */
export { useAuth };
