// profile-cache.ts — REMOVED
// Firebase-based profile caching is no longer needed.
// User session is now maintained via JWT cookie (HTTP-only).
// The auth state is hydrated from GET /api/auth/me on app mount.
// This file is intentionally left as an empty stub to avoid import errors.

export {};
