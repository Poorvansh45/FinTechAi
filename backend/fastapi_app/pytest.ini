[pytest]
testpaths = tests
# The legacy validation script needs Stock_Data.csv (gitignored) and is a manual
# check, not a CI unit test — exclude it from automated collection.
addopts = -q --ignore=tests/test_fvg_redesign.py
